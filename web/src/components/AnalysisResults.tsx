// Compatibility export for older callers. The Results feature owns the
// state-machine and its route-specific presentation.
export {
  AnalysisResults,
  type AnalysisResultsProps,
} from '../features/queries/results/AnalysisResults'
