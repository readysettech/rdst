/**
 * The one value-proposition line for RDST (RDST UX plan, workstream C1 /
 * decision D-5). Persistent chrome (the sidebar) owns this message; other
 * surfaces that used to carry their own competing description import it
 * instead of writing a new one, so a single phrasing describes the whole app
 * (USE-050/051/052).
 */
export const VALUE_PROPOSITION = 'Find slow queries. Prove the fix.'
