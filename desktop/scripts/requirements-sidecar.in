# Build tooling that is not declared by RDST itself. RDST's runtime
# dependencies are compiled from the Python tree's pyproject.toml; run
# `pnpm lock:sidecar`, which locates that tree in either checkout layout.
editables==0.6
hatchling==1.31.0
pip==26.1.2
pyinstaller==6.21.0
