appId: com.readyset.rdst-desktop
productName: RDST Desktop
directories:
  buildResources: resources
  output: dist
files:
  - out/**/*
  - node_modules/**/*
asarUnpack:
  - node_modules/electron-liquid-glass/prebuilds/**/*.node
extraFiles:
  - from: "sidecar/${os}-${arch}"
    to: "sidecar"
    filter:
      - "**/*"
mac:
  artifactName: "rdst-desktop-${version}-${arch}.${ext}"
  category: public.app-category.developer-tools
  target:
    - dmg
    - zip
  # Notarization requires the hardened runtime. Signing credentials come
  # from the environment (CSC_LINK/CSC_KEY_PASSWORD). Notarization is
  # handled outside electron-builder: the CI build script submits the dmg
  # asynchronously and a separate pipeline step polls Apple's verdict, so
  # the build never blocks on the notary queue.
  hardenedRuntime: true
  gatekeeperAssess: false
  notarize: false
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.plist
  # Update channel fed by main-branch CI builds; keep in sync with
  # UPDATE_FEED_URLS in src/main/update-policy.ts.
  publish:
    - provider: generic
      url: https://downloads.readyset.io/packages/rdst-desktop/macos/update
win:
  target:
    - nsis
linux:
  artifactName: "rdst-desktop-${version}-${arch}.${ext}"
  category: Development
  target:
    - AppImage
    - deb
    - rpm
  publish:
    - provider: generic
      url: https://downloads.readyset.io/packages/rdst-desktop/linux/update
# electron-builder's default depends lists plus ALSA: the Electron binary
# links libasound.so.2 at load time, so a stock install without it fails
# to launch. Overriding depends replaces the defaults, hence the full list.
# The ALSA dep is versioned so apt must pick a real alsa-lib (libasound2 or
# the versioned-Provides libasound2t64), never the liboss4-salsa stub whose
# unversioned Provides also claims libasound2.
deb:
  depends:
    - libgtk-3-0
    - libnotify4
    - libnss3
    - libxss1
    - libxtst6
    - xdg-utils
    - libatspi2.0-0
    - libuuid1
    - libsecret-1-0
    - "libasound2 (>= 1.0.17)"
rpm:
  depends:
    - gtk3
    - libnotify
    - nss
    - libXScrnSaver
    - "(libXtst or libXtst6)"
    - xdg-utils
    - at-spi2-core
    - "(libuuid or libuuid1)"
    - alsa-lib
