apiVersion: v1
kind: Secret
metadata:
  name: readyset-{target_name}
  namespace: {namespace}
  labels:
    app: readyset-cache-{target_name}
    managed-by: rdst
type: Opaque
stringData:
  database-url: "{db_engine}://{db_user}:REPLACE_WITH_PASSWORD@{db_host}:{db_port}/{db_name}"
